apiVersion: v1
kind: Pod
metadata:
  name: nginx4
spec:
  containers:
  - image: nginx:alpine
    name: nginx4
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
